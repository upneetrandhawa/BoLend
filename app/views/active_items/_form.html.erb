<%= form_with(model: active_item, local: true) do |form| %>
  <% if active_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(active_item.errors.count, "error") %> prohibited this active_item from being saved:</h2>

      <ul>
      <% active_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
